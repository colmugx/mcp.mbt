import {
  "colmugx/mcp/client",
  "moonbitlang/async",
  "moonbitlang/core/debug",
}

pkgtype(kind: "executable")

// Native CLI example; async runtime entry points are unavailable outside
// native/js/wasm and the CLI is only exercised on native.

supported_targets = "native"
